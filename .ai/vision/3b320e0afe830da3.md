---
source: /Users/fanyunfei/Desktop/个人博客/src/content/posts/性能测试/BPS性能测试分享资料/35-新建-LoadProfile.webp
mode: general
model: glm-5v-turbo
---
```markdown
# Image Description: Application Performance Testing Interface

## Overview
The image displays a web-based user interface for an application performance or load testing tool (likely HP LoadRunner or similar enterprise software). The interface is divided into a navigation header, a breadcrumb trail, a data visualization chart, a configuration panel for test phases, and a footer action bar. A TeamViewer icon is overlaid on the left edge, indicating a remote desktop session.

## 1. Header & Navigation
*   **Top Navigation Bar (Dark Grey Background):**
    *   **Tabs:** `CONTROL CENTER`, `TEST`, `MANAGERS`, `HELP`.
    *   **Right Side Icons:** A close icon (`X`), a notification badge containing the number `1`, and a user profile icon.
*   **Breadcrumb Bar (Green Background):**
    *   **Icon:** A curved arrow indicating navigation history.
    *   **Path:** `Test Workspace >> waf-v4-tuntu-ruishu >> Components >> ApplicationSimulator_1`
    *   **Actions (Right aligned):**
        *   Button labeled `Browse` with a folder icon.
        *   Help icon (`?`).

## 2. Performance Chart (Top Section)
A line graph displaying metrics over time against a light grey gradient background.

*   **Axes:**
    *   **Y-Axis:** Numerical scale from `0` to `10,000` in increments of `1,000`.
    *   **X-Axis:** Time scale from `0` to `180+` in increments of `20`.
*   **Data Series (Lines):**
    *   **Blue Line:** Represents `Sessions Per Second`. It spikes immediately from 0 to approximately 9,800, remains flat, and drops sharply at the end of the timeline (~185s).
    *   **Yellow/Gold Line:** Represents `Max Sessions`. It spikes immediately from 0 to approximately 8,000, remains flat, and drops sharply at the end.
    *   **Green Line:** Represents `Data Rate`. It remains constant at approximately 1,000 throughout the duration.
*   **Legend (Below Chart):**
    *   Radio Button (Selected): `Sessions Per Second`
    *   Radio Button (Unselected): `Max Sessions`
    *   Radio Button (Unselected): `Data Rate`
    *   Radio Button (Selected): `All`

## 3. Configuration Panel: "Phase Settings - Default Mode"
Located below the chart, this section defines the test execution profile using three distinct columns.

### Column 1: RAMP UP
*   **Header:** Green background with white text `RAMP UP`.
*   **Visual Graph:** A small line chart showing a linear increase followed by a plateau.
*   **Controls:**
    *   **Label:** `Ramp Up Behavior:`
    *   **Dropdown:** Set to `Full Open`.
    *   **Label:** `Ramp Up Duration`
    *   **Time Inputs:** Three input boxes containing values `00`, `00`, `03`.
    *   **Sub-labels:** `Hour`, `Minute`, `Second`.
    *   **Disabled Controls (Greyed Out):**
        *   Text: `SVN Only Retry Mode`
        *   Dropdown: `Obey Retry Count`.

### Column 2: STEADY STATE
*   **Header:** Green background with white text `STEADY STATE`.
*   **Visual Graph:** A small line chart showing a flat horizontal line.
*   **Controls:**
    *   **Label:** `Steady-State Behavior:`
    *   **Dropdown:** Set to `Open and Close Sessions`.
    *   **Label:** `Steady-State Time Interval`
    *   **Time Inputs:** Three input boxes containing values `00`, `03`, `00`.
    *   **Sub-labels:** `Hour`, `Minute`, `Second`.

### Column 3: RAMP DOWN
*   **Header:** Green background with white text `RAMP DOWN`.
*   **Visual Graph:** A small line chart showing a plateau followed by a linear decrease.
*   **Controls:**
    *   **Label:** `Ramp Down Behavior:`
    *   **Dropdown:** Set to `Full Close`.
    *   **Label:** `Ramp Down Time Interval`
    *   **Time Inputs:** Three input boxes containing values `00`, `00`, `03`.
    *   **Sub-labels:** `Hour`, `Minute`, `Second`.

## 4. Footer Action Bar
*   **Background:** Dark grey/black strip at the bottom.
*   **Buttons (Right Aligned):**
    *   **Orange/Brown Gradient Button:** Text reads `Create New / Browse`.
    *   **Green Gradient Button:** Text reads `Return to Component Settings`.

## Visual Styling & Layout
*   **Color Palette:** Dominated by dark greys, forest greens (#228B22), and light greys. Accent colors include orange/brown for primary actions and blue/cyan/yellow for chart data.
*   **Layout:** Centered content container with padding. The layout follows a vertical stack: Global Nav -> Context Nav -> Visualization -> Detail Form -> Actions.
*   **Font:** Sans-serif (likely Arial or Verdana), standard web font rendering.
```